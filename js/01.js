let variableSinValor;
let booleano1 = true;
let booleano2 = false;
const PI = 3.14;
const TAU = 2 * PI;
let booleanoAnd = booleano1 && booleano2; // False
let boolenanoNot = !booleano1; // False
let booleanoMix0 = (booleano1 || booleano2) && (booleano1 || (!booleano1 && !booleano2)) // True
